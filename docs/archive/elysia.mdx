---
title: Elysia Integration
description: Use Integrate with Elysia
---

## Installation

Install the Integrate SDK in your Elysia project:

```bash
bun add integrate-sdk
```

## Setup

### 1. Server Configuration

Create a server configuration file with your OAuth credentials:

```typescript
// lib/integrate-server.ts
import {
  createMCPServer,
  githubIntegration,
  gmailIntegration,
} from "integrate-sdk/server";

export const { client: serverClient, handler } = createMCPServer({
  integrations: [
    githubIntegration({
      clientId: process.env.GITHUB_CLIENT_ID!,
      clientSecret: process.env.GITHUB_CLIENT_SECRET!,
      scopes: ["repo", "user"],
    }),
    gmailIntegration({
      clientId: process.env.GMAIL_CLIENT_ID!,
      clientSecret: process.env.GMAIL_CLIENT_SECRET!,
      scopes: ["gmail.send", "gmail.readonly"],
    }),
  ],
});
```

### 2. Mount the Handler

Add OAuth route handlers to your Elysia application:

```typescript
import { Elysia } from "elysia";
import { handler } from "./lib/integrate-server";

const app = new Elysia();

// Register OAuth routes with catch-all
app.on(["POST", "GET"], "/api/integrate/*", (context) => {
  return handler(context.request);
});

app.listen(3000);
```

## Configuration

Add your OAuth credentials to your `.env` file:

```bash
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
```

## CORS Setup

Configure CORS using Elysia's CORS integration:

```typescript
import { cors } from "@elysiajs/cors";

app.use(
  cors({
    origin: "http://localhost:3001",
    credentials: true,
  })
);
```

## Usage Examples

### Client-Side Authorization

```typescript
import { createMCPClient, githubIntegration } from "integrate-sdk";

const client = createMCPClient({
  integrations: [
    githubIntegration({
      scopes: ["repo", "user"],
    }),
  ],
});

// Start OAuth flow
await client.authorize("github");
```
