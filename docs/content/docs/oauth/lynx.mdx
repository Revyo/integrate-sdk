---
title: Lynx Integration
description: Integrate OAuth with Lynx cross-platform framework
---

## Installation

Install the Integrate SDK in your Lynx project:

```bash
bun add integrate-sdk
```

## Mount the Handler

Create a catch-all API route in your Lynx project:

```typescript
// api/auth/[...all].ts
import { toLynxHandler } from 'integrate-sdk/adapters/lynx';

const handler = toLynxHandler({
  providers: {
    github: {
      clientId: process.env.GITHUB_CLIENT_ID!,
      clientSecret: process.env.GITHUB_CLIENT_SECRET!,
      redirectUri: 'lynxapp://oauth/callback',
    },
  },
});

export { handler as GET, handler as POST };
```

## Configuration

Add your OAuth credentials to your `.env` file:

```bash
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
```

## Usage Examples

### Client-Side Authorization

```typescript
import { createMCPClient, githubPlugin } from 'integrate-sdk';

const client = createMCPClient({
  plugins: [
    githubPlugin({
      scopes: ['repo', 'user'],
    }),
  ],
});

// Start OAuth flow
await client.authorize('github');
```

## Framework-Specific Notes

- **Cross-Platform**: Lynx renders natively on Android, iOS, and Web.
- **Native Performance**: Uses native rendering for optimal performance.
- **URL Schemes**: Configure platform-specific URL schemes for OAuth redirects.

