---
title: Introduction
description: A type-safe TypeScript SDK for connecting to the Integrate MCP server
---

import { Cards, Card } from "fumadocs-ui/components/card";

# Integrate SDK

A type-safe TypeScript SDK for connecting to the Integrate MCP (Model Context Protocol) server. Access GitHub, Gmail, Notion, and other integrations through a simple, plugin-based API.

**Server:** `https://mcp.integrate.dev/api/v1/mcp`

## What is this SDK?

This SDK is a **client library** that connects to the Integrate MCP server to access various third-party integrations.

**Key concepts:**

1. **Connect to the Integrate MCP server** - The SDK connects to `https://mcp.integrate.dev/api/v1/mcp`
2. **Configure OAuth credentials** - You provide your own OAuth app credentials for each integration (GitHub, Gmail, etc.)
3. **Call tools** - Execute actions like creating GitHub issues, sending emails, searching Notion pages
4. **OAuth flow happens server-side** - The SDK sends your OAuth config to the server, which handles the actual authentication flow

**Important:** You need to create your own OAuth apps (e.g., GitHub OAuth app, Google OAuth app) and provide the credentials to the SDK. The SDK does not provide OAuth credentials.

## Features

- 🔌 **Plugin-Based Architecture** - Enable only the integrations you need
- 🔒 **Type-Safe** - Full TypeScript support with IntelliSense
- 🌊 **Real-time Communication** - HTTP streaming with NDJSON
- 🔐 **Complete OAuth Flow** - Built-in OAuth 2.0 with PKCE (popup/redirect modes)
- 🌍 **Universal** - Works in browser and Node.js environments
- 🛠️ **Extensible** - Configure plugins for any server-supported integration
- 📦 **Zero Dependencies** - Lightweight implementation

## Get Started

<Cards>
  <Card
    title="Installation"
    href="/docs/getting-started/installation"
    description="Install the SDK in your project"
  />
  <Card
    title="Quick Start"
    href="/docs/getting-started/quick-start"
    description="Get up and running in minutes"
  />
  <Card
    title="OAuth Flow"
    href="/docs/guides/oauth-flow"
    description="Learn how to authorize users with OAuth 2.0"
  />
  <Card
    title="Built-in Plugins"
    href="/docs/plugins"
    description="Explore GitHub, Gmail, and other plugins"
  />
  <Card
    title="Advanced Usage"
    href="/docs/guides/advanced-usage"
    description="Error handling, re-auth, and more"
  />
  <Card
    title="Vercel AI SDK"
    href="/docs/integrations/vercel-ai"
    description="Integrate with Vercel AI SDK"
  />
</Cards>

## How It Works

1. **Client Configuration**: You configure the SDK with plugins for the integrations you want to use (GitHub, Gmail, etc.)
2. **User Authorization (Browser)**: Users authorize your app via OAuth 2.0 (popup or redirect flow)
3. **Token Management**: Session tokens are managed and can be persisted across sessions
4. **Connection**: The SDK connects to `https://mcp.integrate.dev/api/v1/mcp` using HTTP streaming (NDJSON)
5. **Tool Discovery**: The SDK fetches available tools from the server and filters them based on your enabled plugins
6. **Tool Calls**: When you call a tool, the SDK sends a JSON-RPC request with the session token
7. **Server Execution**: The server executes the tool using the stored OAuth credentials

## Browser & Server Support

The SDK works in both environments with different capabilities:

### 🌐 Browser (Client-Side)
- ✅ Full OAuth authorization UI (popup/redirect)
- ✅ Make API calls
- ✅ Session token management
- ✅ Complete user flow

### 🖥️ Node.js (Server-Side)
- ✅ Make API calls with existing token
- ✅ Session token management
- ❌ OAuth UI (throws error - use browser for authorization)
- ✅ Perfect for backend API routes

**Typical pattern:** Handle OAuth in browser, pass token to backend for API calls.

## Server Information

- **Endpoint:** `https://mcp.integrate.dev/api/v1/mcp`
- **Protocol:** MCP (Model Context Protocol) over HTTP streaming
- **Format:** Newline-delimited JSON (NDJSON)
- **Methods:** `initialize`, `tools/list`, `tools/call`
